# iPad で遊べるようにする手順（ホーム画面アプリ化）

このゲームは **PWA（ホーム画面に追加してアプリのように遊べる Web アプリ）** に対応しました。
一度ネットから読み込めば、そのあとは **オフライン（ネットなし）でも起動** できます。

必要な作業は「①ネットに公開 → ②iPad のホーム画面に追加」の2ステップだけです。
（公開はあなたのアカウントでの操作になります。私＝アシスタントは代わりに公開できません。）

---

## ① ゲームを無料でネットに公開する（HTTPS が必要）

いちばん簡単なのは **Netlify Drop**（ドラッグ＆ドロップで公開）です。

1. パソコンの Safari か Chrome で **https://app.netlify.com/drop** を開く。
2. 無料アカウントでログイン（メール or Google でOK）。
3. この **`game` フォルダーごと** ページの点線ワクにドラッグ＆ドロップする。
   - フォルダー内の `index.html` / `game.js` / `assets.js` / `manifest.webmanifest` / `sw.js` / `icon-*.png` などが全部必要なので、**フォルダーまるごと**入れてください。
4. 数秒で `https://○○○.netlify.app` のような **URL** が発行されます。これがゲームのアドレスです。

> 他の方法でもOK：GitHub Pages、Cloudflare Pages、tiiny.host など。**HTTPS で配信されること**だけが条件です（オフライン機能に必須）。

---

## ② iPad の Safari でホーム画面に追加する

1. iPad の **Safari** で、①で発行された URL を開く。
2. 画面が出たら一度「▶ スタート」で遊べることを確認（初回だけネット必要）。
3. 画面上の **共有ボタン（□に↑）** をタップ → **「ホーム画面に追加」** を選ぶ → 「追加」。
4. ホーム画面に **ネコの騎士のアイコン** が出ます。それをタップすると、
   - **フルスクリーン**（Safari のバーなし）で起動
   - **横向き**で表示
   - **オフラインでも起動**（1回ネットで開いたあとなら、機内モードでもOK）

---

## 遊び方（iPad）

- 画面下の **◀ ▶** で移動、**⤴** ジャンプ、**⚔** こうげき、**🛡** ガード。
- 右上の **⏸ ボタン**でポーズ。ポーズ画面から **▶ つづける / 🔄 やりなおす / 🏘 むらへもどる** が選べます（キーボードの P・R キーの代わり）。
- たてに持つと「iPadを よこむきに してね」の案内が出ます（ホーム画面アプリなら自動で横向き）。

---

## ゲームを更新したとき（新キャラ追加など）

1. `assets/*.svg` を変えたら `node build_assets.js` を実行（いつも通り）。
2. **`sw.js` の `VERSION`（`'v1'`）の数字を1つ上げる**（例：`'v2'`）。← これで古いキャッシュを作り直します。
3. 公開先に**もう一度アップロード**（Netlify なら再度ドラッグ＆ドロップ、または同じサイトに Deploy）。
4. iPad のアプリを1回オンラインで開くと、新しい内容に自動で入れ替わります。

> `VERSION` を上げ忘れると、iPad 側が古い画面のままになることがあります（そのときはアプリを開き直す／オンラインでもう一度開くと直ります）。

---

## 公開せずに、まず家で試したいとき（おまけ・任意）

同じ Wi-Fi なら、Mac のサーバーを起動して iPad の Safari から開けます（お試し用）。

1. Mac で：`cd "…/百花ゲーム01/game" && python3 -m http.server 8123`
2. iPad の Safari で：`http://192.168.10.103:8123` を開く（Mac の IP。変わることがあります）。

※ この方法は **Mac が起動中・同じ Wi-Fi のときだけ**。また http（暗号化なし）なので **オフライン機能とホーム画面アプリのキャッシュは無効**です。本番は①の HTTPS 公開を使ってください。
