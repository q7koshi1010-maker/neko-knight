# アート規格書（全スプライト共通・厳守）

もとは子どもが描いた手描きの「ゆるかわ」キャラクター。その温かみを保ちつつ、
クリーンなSVGベクターに描き直す。全員この規格を厳守すること。

## 出力フォーマット
- 各アセットは **単体の .svg ファイル**。
- ルート: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">` の形。
  **`width`/`height` 属性は付けない**（拡大縮小できるように）。
- 背景は**透明**（背景の矩形を置かない）。
- ファイル内に外部参照（外部画像/フォント）を入れない。フィルタは軽微なもの（soft shadow）のみ可、無くてもよい。
- 文字（日本語テキスト）はスプライトに入れない。

## 最優先ルール：元絵に忠実に
このプロジェクトは「元の手描きイラストに忠実」であることが最優先。
勝手にカッコよく／可愛く整えすぎない。元絵の**プロポーション・パーツ・色・クセ**
（例：目や口の有無、手足の形、持ち物の向き）をそのまま再現する。
渡された参照画像（refs/*.png）を必ずReadして、形をよく観察してから描くこと。
線はベクターでクリーンにしてよいが、**造形は元絵どおり**にする。

## 絵柄ルール（ゆるかわ手描き風）
- 輪郭線：濃い茶色 `#4a3728`、`stroke-width` は 3〜3.5、`stroke-linejoin="round"` `stroke-linecap="round"`。少しだけ丸っこく、こわばらない線。
- 塗り：フラットなパステル。ベタ塗りでよいが、軽い陰影（少し濃い同系色を体の下側に）を入れると可愛い。
- 目：黒い丸ドット＋小さな白ハイライト。表情はシンプルに。
- ほっぺ：薄いピンクの丸みチーク（任意）。
- 全体に**手描きのゆるさ**を残す（左右完全対称にしすぎない）。

## 配置ルール
- キャラは viewBox の中で大きめに、周囲 8〜12px の余白。
- **正面ではなく右向き**に描く（進行方向＝右）。エンジン側で必要に応じて左右反転する。
- 地上キャラ（足のあるもの）は**足の底を y≈122 付近**（下辺近く）に置く。
- 空中キャラ（浮遊）は viewBox 中央に配置。

## 共通パレット
- 輪郭: `#4a3728`
- ネコ白: `#fdfdfb` / 影 `#e7e4dc`
- 金属（剣・盾）: `#c9ccd4` / 影 `#9aa0ab` / ハイライト `#eef1f6`
- 盾の地: `#3a4a5a`、盾の縁: `#c9a24a`
- 星/金貨: `#ffd23f` / 縁 `#e8a200`
- サメ: `#8fb8c9` / 腹 `#dff0f4`
- おばけ: `#f4f6fb` / 影 `#dfe3ee`
- コウモリ: `#a98bd0` / 翼影 `#8b6cb8`
- 炎の悪魔: 体 `#2b2b33` / 炎 `#ff8a1e`＋`#ffd23f`
- 魔法使い: ローブ `#5b4a8a` / 帽子 `#3f3468` / 肌 `#f4d9b8`
- 雲: `#c9cdd6` / 影 `#a7acb8`
- 死神: ローブ `#1f2028` / ドクロ `#f2f0e6` / 大鎌の柄 `#c9a24a`・刃 `#c9ccd4`
